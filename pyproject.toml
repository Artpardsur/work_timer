[project]
name = "work-timer"
version = "2.0.0"
description = "Напоминания о перерывах, разминке и моргании во время работы за компьютером"
requires-python = ">=3.9"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
pythonpath = ["."]
